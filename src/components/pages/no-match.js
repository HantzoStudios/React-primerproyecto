import React from 'react';
import { Link } from 'react-router-dom';

export default function() {
    return(
        <div>
            <h2>Pagina no encontrada mendrugo</h2>
            <Link to="/">return to Homepage</Link>
        </div>
    );
}